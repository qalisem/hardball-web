import React from 'react';
import ReactDOM from 'react-dom/client';
import Hardball from './Hardball.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Hardball />
  </React.StrictMode>
);
